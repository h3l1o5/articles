---
title: "建構無向量 RAG 系統（不用 Embeddings、不用 Vector DB）"
date: 2026-03-28
categories: [AI]
tags: [RAG, LLM, Python]
image:
  path: /assets/img/posts/2026-03-28-vectorless-rag-system/banner.jpg
  alt: "Building Vectorless RAG System"
---

> 原文：[Building Vectorless RAG System (No Embeddings, No Vector DB)](https://x.com/TheVixhal/status/2037236399691489797) by **vixhaℓ** (@TheVixhal)
> 發佈日期：2026 年 3 月 27 日

---

在這篇文章中，我們要建構一個無向量、基於推理的 RAG 系統，使用階層式頁面索引（hierarchical page indexing）。文件會被轉換成一棵樹，然後由 LLM 在這棵樹中推理找到答案。不需要 embeddings，也不需要 similarity search。

這跟我們在現實生活中搜尋資訊的方式非常相似。當你想在教科書中找某個東西時，你不會從頭開始逐頁閱讀。你會打開目錄，找到正確的章節，查看裡面的小節，然後直接翻到你需要的那一節。

PageIndex 的運作方式完全一樣。你給它一份文件，它會從文件中建構一棵樹——每個分支是一個章節，每個葉節點是實際的文字內容——然後當你問它問題時，LLM 會逐層導航這棵樹來找到正確的答案。

完整程式碼：[https://github.com/vixhal-baraiya/pageindex-rag](https://github.com/vixhal-baraiya/pageindex-rag)

## 計畫

在我們寫任何一行程式碼之前，先看看完整的計畫。

**Step 1：將文件解析為階層式樹狀結構。**
我們把文件送給 LLM，要求它將文字拆分成頂層章節。然後對每個夠長、值得進一步拆分的章節，再次送給 LLM 取得子章節。這給了我們一棵多層的樹。短章節保留為葉節點，長章節變成帶有子節點的內部節點。

**Step 2：由下而上為每個節點生成摘要。**
我們從葉節點往根節點遍歷這棵樹。每個葉節點會得到一個由 LLM 生成的短摘要，基於它的原始文字。每個內部節點會得到一個由其子節點摘要組合而成的摘要。根節點最終會包含整份文件的摘要。

**Step 3：儲存索引。**
我們將樹序列化為 JSON 檔案。這就是索引。我們只需建構一次就能重複使用。

**Step 4：透過遍歷樹來檢索。**
在查詢時，我們從根節點開始。我們向 LLM 展示所有子節點的摘要，並詢問應該進入哪一個。我們移動到那個子節點，然後重複這個過程，直到到達葉節點。葉節點的原始文字就是我們檢索到的 context。

**Step 5：生成答案。**
我們將檢索到的 context 和問題一起傳給 LLM，取得答案。

## 架構

讓我們看看資料如何在系統中流動。

### Index Time（只執行一次）

![Architecture - Index Time](/assets/img/posts/2026-03-28-vectorless-rag-system/architecture-index.jpg)

### Query Time（每次提問都會執行）

![Architecture - Query Time](/assets/img/posts/2026-03-28-vectorless-rag-system/architecture-query.jpg)

現在我們知道要建構什麼、所有部件如何組合在一起了，讓我們開始寫程式碼。

## Step 1：建立專案結構

```
pageindex-rag/
    pageindex/
        __init__.py
        node.py
        parser.py
        indexer.py
        retriever.py
        storage.py
    main.py
    document.md
```

建立目錄：

```bash
mkdir pageindex-rag
cd pageindex-rag
mkdir pageindex
touch pageindex/__init__.py
```

## Step 2：定義 Node（pageindex/node.py）

文件的每個章節都會變成一個 PageNode。它儲存標題、原始文字、稍後生成的摘要，以及它的子節點。

```python
from dataclasses import dataclass, field
from typing import Optional


@dataclass
class PageNode:
    title: str
    content: str        # raw text, populated at leaves
    summary: str        # generated by LLM, populated by indexer
    depth: int          # 0 = root, 1 = section, 2 = subsection
    children: list = field(default_factory=list)
    parent: Optional["PageNode"] = None

    def is_leaf(self) -> bool:
        return len(self.children) == 0
```

## Step 3：解析文件（pageindex/parser.py）

我們分兩次建構這棵樹。首先，我們要求 LLM 將整份文件拆分成頂層章節。然後，對任何夠長、值得進一步拆分的章節（超過 300 個字），我們再次送給 LLM 取得子章節。短章節保留為葉節點，長章節變成帶有子節點的內部節點。

`_segment` 是負責單層拆分的輔助函式。`parse_document` 呼叫它兩次：一次針對整份文件，一次針對每個長章節。

```python
import json
import openai
from .node import PageNode

client = openai.OpenAI()

SUBSECTION_THRESHOLD = 300  # words


def _segment(text: str) -> list:
    prompt = f"""Split the following text into logical sections.
Return a JSON object with a "sections" key. Each item has:
- "title": short title (5 words or less)
- "content": the text belonging to this section

Text:
{text[:8000]}"""

    response = client.chat.completions.create(
        model="gpt-5.4",
        messages=[{"role": "user", "content": prompt}],
        max_completion_tokens=3000,
        response_format={"type": "json_object"},
    )
    parsed = json.loads(response.choices[0].message.content)
    return parsed.get("sections", [])


def parse_document(text: str) -> PageNode:
    root = PageNode(title="root", content="", summary="", depth=0)

    for item in _segment(text):
        title = item.get("title", "Section")
        content = item.get("content", "")

        node = PageNode(title=title, content="", summary="", depth=1)
        node.parent = root

        word_count = len(content.split())
        if word_count > SUBSECTION_THRESHOLD:
            subsections = _segment(content)
            if len(subsections) > 1:
                for sub in subsections:
                    child = PageNode(
                        title=sub.get("title", "Subsection"),
                        content=sub.get("content", ""),
                        summary="",
                        depth=2,
                    )
                    child.parent = node
                    node.children.append(child)
            else:
                node.content = content
        else:
            node.content = content

        root.children.append(node)

    return root
```

完成後，短章節是帶有 content 的葉節點，長章節是帶有子章節子節點的內部節點。此時所有 summary 欄位都是空的，indexer 接下來會填入這些值。

## Step 4：建構摘要（pageindex/indexer.py）

我們以 post-order（子節點先於父節點）遍歷這棵樹。每個葉節點會摘要自己的內容，每個內部節點會得到由其子節點摘要組成的摘要。Post-order 保證每個子節點在其父節點需要之前就已經有了摘要。

```python
import openai
from .node import PageNode

client = openai.OpenAI()


def _summarize(text: str, section_name: str = "") -> str:
    hint = f"This is the section titled: {section_name}.\n" if section_name else ""
    prompt = f"""{hint}Summarize the following in 2-3 sentences. Be specific and factual. Do not add anything not in the text.

{text[:3000]}"""
    response = client.chat.completions.create(
        model="gpt-5.4-mini",
        messages=[{"role": "user", "content": prompt}],
        max_completion_tokens=150,
    )
    return response.choices[0].message.content.strip()


def build_summaries(node: PageNode):
    for child in node.children:
        build_summaries(child)

    if node.is_leaf():
        if node.content.strip():
            node.summary = _summarize(node.content, node.title)
        else:
            node.summary = "(empty section)"
    else:
        children_text = "\n\n".join(
            f"[{c.title}]: {c.summary}" for c in node.children
        )
        node.summary = _summarize(children_text, node.title)
```

執行 `build_summaries(root)` 之後，樹中的每個節點都會有一個有意義的摘要。

## Step 5：儲存與載入索引（pageindex/storage.py）

我們將樹序列化為 JSON，這樣只需要建構一次。

```python
import json
from .node import PageNode


def save(node: PageNode, path: str):
    def to_dict(n: PageNode) -> dict:
        return {
            "title": n.title,
            "content": n.content,
            "summary": n.summary,
            "depth": n.depth,
            "children": [to_dict(c) for c in n.children],
        }
    with open(path, "w") as f:
        json.dump(to_dict(node), f, indent=2)


def load(path: str) -> PageNode:
    def from_dict(d: dict) -> PageNode:
        node = PageNode(
            title=d["title"],
            content=d["content"],
            summary=d["summary"],
            depth=d["depth"],
        )
        for child_dict in d["children"]:
            child = from_dict(child_dict)
            child.parent = node
            node.children.append(child)
        return node
    with open(path) as f:
        return from_dict(json.load(f))
```

## Step 6：透過樹搜尋進行檢索（pageindex/retriever.py）

從根節點開始，LLM 閱讀子節點的摘要並選擇最佳分支。如果那個子節點是內部節點，我們在該層重複操作。我們持續前進直到命中葉節點。

```python
import openai
from .node import PageNode

client = openai.OpenAI()


def _pick_child(query: str, node: PageNode) -> PageNode:
    options = "\n".join(
        f"{i + 1}. [{c.title}]: {c.summary}"
        for i, c in enumerate(node.children)
    )
    prompt = f"""You are navigating a document tree to find the answer to a question.

Current section: "{node.title}"
Question: {query}

Children of this section:
{options}

Which child section most likely contains the answer? Reply with only the number."""

    response = client.chat.completions.create(
        model="gpt-5.4-mini",
        messages=[{"role": "user", "content": prompt}],
        max_completion_tokens=5,
    )
    try:
        index = int(response.choices[0].message.content.strip()) - 1
        return node.children[index]
    except (ValueError, IndexError):
        return node.children[0]


def retrieve(query: str, root: PageNode) -> str:
    node = root
    while not node.is_leaf():
        if not node.children:
            break
        node = _pick_child(query, node)
    return node.content
```

## Step 7：整合所有部分（main.py）

```python
import os
from pageindex.parser import parse_document
from pageindex.indexer import build_summaries
from pageindex.retriever import retrieve
from pageindex import storage
import openai

client = openai.OpenAI()
INDEX_PATH = "index.json"


def build_index(doc_path: str):
    print("Parsing document...")
    text = open(doc_path).read()
    tree = parse_document(text)

    print("Building summaries (this makes LLM calls)...")
    build_summaries(tree)

    print(f"Saving index to {INDEX_PATH}")
    storage.save(tree, INDEX_PATH)
    return tree


def ask(query: str) -> str:
    if not os.path.exists(INDEX_PATH):
        raise FileNotFoundError("Index not found. Run build_index() first.")

    tree = storage.load(INDEX_PATH)
    context = retrieve(query, tree)

    response = client.chat.completions.create(
        model="gpt-5.4",
        messages=[{
            "role": "user",
            "content": f"Answer using only the context below.\n\nContext:\n{context}\n\nQuestion: {query}"
        }],
        max_completion_tokens=500,
    )
    return response.choices[0].message.content.strip()


if __name__ == "__main__":
    build_index("document.md")
    print(ask("Your Question"))
```

## 索引的樣貌

執行 `build_index` 之後，打開 `index.json`，你會看到類似這樣的內容：

```json
{
  "title": "root",
  "summary": "Document covers returns, shipping options, and account setup.",
  "content": "",
  "depth": 0,
  "children": [
    {
      "title": "Returns and Refunds",
      "summary": "Refunds are processed within 14 days of receiving the returned item.",
      "content": "We accept returns within 30 days...",
      "depth": 1,
      "children": []
    },
    {
      "title": "Shipping Options",
      "summary": "Covers domestic (3-5 days) and international shipping (7-14 days).",
      "content": "",
      "depth": 1,
      "children": [
        {
          "title": "Domestic Shipping",
          "summary": "Standard delivery takes 3-5 business days via USPS.",
          "content": "We ship domestically via USPS...",
          "depth": 2,
          "children": []
        },
        {
          "title": "International Shipping",
          "summary": "International orders ship via DHL and arrive in 7-14 days.",
          "content": "International shipping is available to 50+ countries...",
          "depth": 2,
          "children": []
        }
      ]
    },
    {
      "title": "Account Setup",
      "summary": "Instructions for creating and verifying a new account.",
      "content": "To create an account, visit...",
      "depth": 1,
      "children": []
    }
  ]
}
```

短章節保留為 depth 1 的葉節點。長章節變成帶有 depth 2 子章節子節點的內部節點。檢索時逐層導航直到命中葉節點。

## 常見問題

**LLM 一直選錯分支。** 你的摘要太模糊了。試著在 `_summarize` 中使用更強的模型，或在 prompt 中加入更多細節。

**LLM 分段在不好的位置切開章節。** 當 `parse_document` 處理長文件時，有時會在思路中間切斷。修正方法是增加分段呼叫中的 `max_tokens`，或在送出前先將文件分成約 3000 字的區塊。

**葉節點的內容太長。** 如果一個葉節點有超過約 1500 個 token 的內容，降低 `SUBSECTION_THRESHOLD` 讓更多章節被拆分成子章節。

完整程式碼：[https://github.com/vixhal-baraiya/pageindex-rag](https://github.com/vixhal-baraiya/pageindex-rag)
